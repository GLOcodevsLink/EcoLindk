<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Gemini est appelé par HTTPS depuis GeminiService. Sans cette
         permission, Android bloque la requête avant qu'elle n'atteigne l'API. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Réglage "rapide" de l'onboarding : notifications (Android 13+), voir
         onboarding_screen.dart. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Module Fournisseur de déchets : photo du déchet posté (PostWasteScreen)
         et position GPS demandée au moment de créer une demande de collecte
         seulement — jamais à l'inscription (voir GeoHelper). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Speech-to-text dans l'Assistant IA (AiAssistantScreen) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:label="ecolindk"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Sans ces deux entrées, Android 11+ (API 30+) cache le moteur de
             reconnaissance vocale du système à l'app (restriction de
             "package visibility") : speech_to_text.initialize() renvoie
             silencieusement `false`, donc le bouton micro de
             AiAssistantScreen ne s'affiche jamais — symptôme exact de "la
             saisie vocale ne marche pas" sans aucune erreur visible. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
    </queries>
</manifest>
